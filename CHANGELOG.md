# CHANGELOG

날짜 내림차순. 사용자에게 보이는 변화와 그 원인을 적는다.
설계 판단의 근거는 `.claude/docs/DESIGN.md`, 재개 지점은
`.claude/docs/SPRINT1-CHECKPOINT.md`.

## 2026-07-31

### 고침

- **브리핑 4개 카드 중 3개가 항상 비어 있던 문제.** `match_document_chunks`
  가 HTTP 200 에 0행을 돌려주고 있었다. 함수에 임계값이 없고 컬렉션에 청크가
  521개 있는데도 그랬다. 원인은 `document_chunks_embedding_idx` — ivfflat
  `lists=100` 에 500여 행이라 리스트당 약 5행인데 `ivfflat.probes` 기본값이 1
  이라, 리스트 하나만 훑고 거기에 `collection_name` 필터를 걸면 남는 게 없었다.
  같은 질문을 정확 검색으로 돌리면 유사도 0.5 대 결과가 정상으로 나온다.
  → `scripts/rag_recall_migration.sql` **(Supabase SQL Editor 에서 실행 필요)**
  로 인덱스를 내리고, 함수에 `probes = 10` 을 박아 나중에 인덱스를 되살려도
  recall 이 무너지지 않게 했다. 되살리는 기준은 마이그레이션 주석에 있다.
- **브리핑 검색어가 키워드 나열이라 순위가 흔들리던 문제.**
  ("리스크 문제 이슈 위험" 같은 형태) 임베딩 모델이 문장 학습이라 키워드 더미는
  어떤 청크와도 어중간하게 닮는다. 문장으로 바꿨다. 도메인 컨텍스트를 검색어에
  얹는 것도 시도했으나, 컬럼명이 늘어선 헤더 청크로 끌려가 리스크 카드가 이상치
  행을 놓쳐서 되돌렸다(프롬프트에는 그대로 쓴다).
- **스키마 그래프가 컨테이너에 안 맞아 노드가 잘리던 문제.** `network.fit()`
  호출이 아예 없었다. 그래프 탭이 `hidden` 안에서 먼저 마운트돼 캔버스가 0x0 일
  때 fit 이 돌면 그 배율이 그대로 남으므로, `ResizeObserver` 로 컨테이너가 실제
  크기를 얻을 때 다시 맞춘다. `#mynetwork` 도 pyvis 기본 580px 고정에서 iframe
  높이를 다 쓰도록 바꿨다.
- **한글 라벨이 뭉개지던 문제.** `font-data`(IBM Plex Mono)를 latin 서브셋으로
  받아 한글 글리프가 없었고, 시스템 mono 로 떨어져 전각 칸에 갇혔다. 라벨은
  본문서체 11px 로 옮기고 `font-data` 는 테이블명·수치·ID 같은 ASCII 전용으로
  되돌렸다.
- **`FACT\_RETURN` 처럼 백슬래시가 노출되던 문제.** Claude 가 식별자의 밑줄을
  이스케이프해서 보낸다. 마크다운 렌더러가 이제 되돌린다.
- 같은 두 테이블을 잇는 FK 가 둘 이상일 때 나중 것이 앞의 것을 덮어쓰던 문제.
  그래프가 DiGraph 라 엣지가 하나뿐이어서 조인 키가 소리 없이 사라졌다.

### 더함

- **조인 키 매트릭스** (브리핑 탭). 행이 참조하는 테이블, 열이 참조받는 테이블,
  칸이 그 둘을 잇는 외래키다. 열은 참조 많은 순이라 첫 열이 이 스키마의 축이고,
  강조해 둔다. 빈 칸은 연결이 없다는 뜻이다 — 키를 모르는 연결은 칠하지 않는다.
- `/api/graph/data` 응답의 각 엣지에 `join_key`·`join_keys`. `join_key` 가
  없던 시절에 저장된 그래프는 `relation` 에서 끌어오므로 재적재가 필요 없다.

### 바뀜

- **"지식 그래프" 탭 → "군집 보기".** 조인 키 조회는 매트릭스가 맡는다.
  그래프는 묶음 구조를 보는 보조 뷰다.
- 스키마 그래프가 방향 기준 계층 배치로 고정됐다(physics off). 팩트가 위,
  참조받는 마스터가 아래다. 열 때마다 같은 그림이 나온다.
- `<html lang>` 이 `en` → `ko`.

## 2026-07-30

- `/app` 전면 디자인 패스(작업지시서 컨셉, 라이트 테마, 폰트 3역할, 참조 허브).
- 데모 데이터 재생성(`scripts/generate_beauty.py`, 51행 → 3,392행).
- KG FK 엣지가 파일 처리 순서에 따라 0개가 되던 버그 수정
  (`link_csv_tables()` 후처리 패스 신설).
- 주입 JS 가 통째로 죽어 있던 버그 수정(파이썬 일반 문자열이라 JS 안의 `'\n'`
  이 실제 개행으로 치환돼 SyntaxError → raw 문자열로 변경).
